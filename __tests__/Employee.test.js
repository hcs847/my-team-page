const Employee = require('../lib/Employee');

test('creates an employee object', () => {

});

test('name is a string', () => {

});

test('id is a number', () => {

});

test('email is in the right format', () => {

});

test('employee is returned', () => {

});